<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".CtomPlayerApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_ctom_player"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_ctom_player"
        android:supportsRtl="true"
        android:theme="@style/Theme.CtomPlayer">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".widget.LiquidNowPlayingWidget"
            android:exported="true"
            android:label="@string/widget_now_playing">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_now_playing_info" />
        </receiver>
        <receiver
            android:name=".widget.OceanMiniPlayerWidget"
            android:exported="true"
            android:label="@string/widget_mini_player">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_mini_player_info" />
        </receiver>
        <receiver
            android:name=".widget.DeepOceanVideoWidget"
            android:exported="true"
            android:label="@string/widget_video">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_video_info" />
        </receiver>
        <receiver
            android:name=".widget.LiquidEqualizerWidget"
            android:exported="true"
            android:label="@string/widget_equalizer">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_equalizer_info" />
        </receiver>
        <receiver
            android:name=".widget.ControlCenterWidget"
            android:exported="true"
            android:label="@string/widget_control_center">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_control_center_info" />
        </receiver>
    </application>
</manifest>